"use client";

/**
 * Smooth scroll placeholder — Lenis was causing incomplete scroll issues
 * (page wouldn't scroll all the way down). Native CSS smooth scrolling
 * via `scroll-behavior: smooth` on <html> is used instead.
 */
export function SmoothScroll() {
  return null;
}
